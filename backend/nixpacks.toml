[phases.setup]
aptPkgs = ['python3', 'python3-pip', 'python3-dev', 'build-essential', 'libpq-dev']

[phases.install]
cmds = ['python3 -m pip install --upgrade pip', 'python3 -m pip install -r requirements.txt']

[start]
cmd = "gunicorn run:app"